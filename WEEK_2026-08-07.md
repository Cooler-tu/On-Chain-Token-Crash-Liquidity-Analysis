# 本周任务计划（2026-08-07 ～ 2026-08-13）

> 来源：导师对 dashboard 需求清单的评论、上一轮持有人口径讨论、仓库现状（README / HANDOVER / NEXT_WEEK / plan）。

## 〇、当前进度总览（2026-08-07 更新）

| 任务 | 状态 | 说明 |
|---|---|---|
| 价格 timeline（V3/V4） | 已完成 | `tvl_timeline.json` 已覆盖 V3/V4，新增 `price_usd`，dashboard 有价格图 |
| Trading volume 按池贡献 | 已完成 | `volume_timeline.json` + dashboard 堆叠柱状图 + 每池占比 |
| Holder 余额口径 | 已完成 | `balance_start / balance_end / net_change / peak_balance` + dashboard 展示（Dune 历史快照 + 重点地址事件流） |
| Pool concentration / main pool 定义 | 已完成 | dashboard 同时展示 TVL 主池、成交量主池、每池 TVL/Volume 明细 |
| Pool TVL timeline 分池 | 已完成（交互待补） | 分池多线图 + 明细表已完成；点击数据点看各池明细尚未做 |
| Liquidity removal detection | 部分完成 | 撤回事件表已上 dashboard；按池、按 USD 归一待做 |
| Large wallet transaction tracking | 部分完成 | Top Movers 已按钱包聚合买卖；USD 阈值与池比例口径待做 |
| LP provider analysis | 已完成（探索原型） | `scripts/lp_correlation.py` + `docs/LP_CORRELATION_DESIGN.md` 已产出 |
| 地址关联 / 钱包聚类 / 资金流 | 部分完成 | 可行性文档 + `fund_flow.json` 原型已完成；钱包聚类待做 |

## 一、本周目标

1. 把 dashboard 的「数据口径」先定清楚：价格、成交量、持有人余额、main pool、分池 TVL 各自用什么定义。
2. 补齐 V3/V4 价格估算和分池 TVL timeline，让 dashboard 不再只有 V2 价格、只有一条总 TVL 曲线。
3. 落地成交量、大额钱包、撤回事件、LP 排行等分析视图。
4. 做一个 LP 相关性分析原型，验证「TVL / LP 数量 / 池成交量」之间是否存在领先或滞后关系。
5. 评估地址关联（EOA + 合约、钱包聚类、资金流）的可行性与数据成本，产出设计文档，不急着上线。

## 二、导师评论评估

| 导师评论 | 评估 | 本周动作 |
|---|---|---|
| Trading volume 建议算每个池对总成交量的贡献 | 采纳。swaps 已有，缺的是按池 / 时间桶汇总 | 新增 `volume_in_token` / `volume_usd`，输出每池贡献占比 |
| Holder 为什么不直接查两次余额，需要评估性能和成本 | 部分采纳。查两次只能拿到净变动，拿不到峰值和轨迹；事件流重建可以在一次期初快照后复用已有 Transfer 事件 | 写 `docs/HOLDER_BALANCE_DESIGN.md`，用真实数据对比成本，再定实现 |
| Main pool 需要再详细解释定义 | 采纳。当前 main pool = TVL 最大的池，不等于官方主池，也不等于成交量主池 | dashboard 同时展示 TVL 主池、成交量主池、每池明细 |
| TVL timeline 分池，多条曲线放一起，点击看具体时间点的 pool 明细 | 采纳。当前数据层只支持 V2/V3 timeline，V4/Curve/Balancer 先补 | dashboard 改成总览 + 每池多线图，点击数据点显示该时间点各池 TVL |
| Liquidity removal detection | 继续做。现有指标把 token0 + token1 直接相加，容易高估 | 改成按池、按目标 token / USD 归一，输出撤回事件表和净撤出 |
| 大额钱包：先找大额，再按钱包聚合买卖 | 采纳。现有只是 raw > 10^18 的粗计数 | 按 USD / 池比例定义大额，再聚合钱包级净买卖 |
| LP 分析不能只看涨跌绝对值，建议做相关系数和时间 shift | 采纳，作为探索方向。同时保留真实 LP 事件作为因果证据 | 输出相关性矩阵 + 领先滞后分析原型 |
| 地址关联（transfer、合约创建）用 Dune 也能快速做 | 可行但需管理预期：聚类是概率性关联，不是身份确认 | 先做数据可行性评估和 `docs/ADDRESS_ASSOCIATION_DESIGN.md` |

## 三、任务拆解

### 1. 价格 timeline：补 V3 / V4

> 状态：已完成（2026-08-07）

现状：

- V2 有 `estimate_price_v2`。
- V3 timeline 里 `price` 全是 0。
- V4 没有进入 `build_tvl_timeline`。

要做的：

- V3：在事件块读 `slot0.sqrtPriceX96`，或从 Dune `dex.trades` 的成交价推导。
- V4：先让 timeline 覆盖 V4 事件，再补价格。
- dashboard 画价格线，并把「AMM 估算价」和「实际成交价」区分开。

验收：

- `tvl_timeline.json` 中每个池都有非 0 价格（无法估算的池要写明原因）。
- dashboard 有价格图，V4 池不再从 timeline 消失。

### 2. Trading volume：按池贡献

> 状态：已完成（2026-08-07）

要做的：

- 从 `swaps.json` 汇总 `volume_in_token`（目标 token 口径）和 `volume_usd`（优先用稳定币 / WETH 作为 quote，其余池用跨池价格）。
- 按时间桶（默认 1 小时）和池两个维度聚合。
- 输出 `volume_timeline.json`，dashboard 用堆叠柱状图或分池线图展示每池贡献。

验收：

- dashboard 能看出「总成交量主要来自哪个池」。
- 报告中有每池成交量占比，和 TVL 占比放一起比较。

### 3. Holder 余额：两种方案先做成本对比

> 状态：已完成（2026-08-08）— Dune 历史快照 + 双时间点字段 + dashboard 已接入。

现状：

- 只查 `to_block` 一次余额，排序后生成 Top Holders。
- RPC 有 80 个左右余额查询上限，uPEG 输出里 3075 个地址中有 2995 个根本没查余额。
- Dune 用的是 `balances_ethereum.latest`，不是历史快照。

要比较的方案：

| 方案 | 查询成本 | 能拿到什么 | 主要问题 |
|---|---|---|---|
| 双时间点快照（from + to） | 约 2N 次 `balanceOf`，N 为地址数 | 期初、期末、净变动 | 拿不到峰值和轨迹；免费 RPC 下 2N 次不可行，需要 Dune 历史余额表 |
| 事件流重建 | 1 次期初快照 + 复用已索引 Transfer 事件 | 余额轨迹、峰值、净变动、持仓时间、holder 数量变化 | 依赖期初快照；没在窗口内出现过的持有人无法覆盖；合约 / router 转账需要归一 |
| 时间桶快照 | N × 桶数，最贵 | 最直观 | 成本最高，只适合少量重点地址 |

结论建议：

- 默认用「双时间点快照」给出全部地址的净变动，成本可接受时再加。
- 对 Top Movers / 重点大户，用「事件流重建」补余额轨迹和峰值。
- dashboard 表头必须标注快照块，区分 `Unique Transfer Addresses`、`Holders at Block X`、`Active Holders`。

交付：

- `docs/HOLDER_BALANCE_DESIGN.md`，记录成本数据、最终口径、抽样偏差说明。
- `holdings.json` 增加 `balance_start`、`balance_end`、`net_change`、`peak_balance` 等可选字段。

### 4. Pool concentration：main pool 定义

> 状态：已完成（2026-08-07）

要做的：

- 文档明确：`main_pool` = 当前快照下 TVL 最大的池，`main_pool_share` = 该池 TVL / 所有有效池 TVL。
- dashboard 增加「每池 TVL + 占比」表。
- 新增 `volume_main_pool`，判断 TVL 主池和成交量主池是否一致。
- 对集中度给阈值提示：单一池 TVL 占比 > 70% 标黄，> 90% 标红。

### 5. Pool TVL timeline：总览 + 分池

> 状态：已完成图表与明细表；「点击数据点显示各池明细」待补。

要做的：

- 先修数据层：`build_tvl_timeline` 支持 V4、Curve、Balancer。
- dashboard 主图：总 TVL 一条线 + 每个池一条线，可切换隐藏。
- 点击图上的数据点：显示该时间点每个池的 TVL、占比、价格、成交量。
- TVL 单位统一显示为「目标 token 数量」或 USD，二选一并在图上注明。

验收：

- 一个多池 token 的 dashboard 能清楚看出 TVL 风险来自哪个池。

### 6. Liquidity removal detection

> 状态：部分完成 — dashboard 已有撤回事件表；按池/USD 归一待做。

要做的：

- 撤回事件按池聚合，计算每个池的净撤出。
- 金额用目标 token 或 USD 归一，不再直接 token0 + token1。
- 增加「撤回事件表」：时间、池、actor、金额、占池 TVL 比例。
- 把「撤出是否伴随价格下跌」作为独立信号，而不是只看撤出金额。

### 7. Large wallet transaction tracking

> 状态：部分完成 — Top Movers 已实现；USD/池比例阈值待做。

要做的：

- 定义大额：按 USD 阈值（例如 $10k / $100k）或占池成交量比例，不能用 raw `10^18` 这种依赖 decimals 的固定值。
- 先找大额 swap / transfer，再按钱包聚合买入、卖出、净额。
- dashboard 新增 Top Movers：按 `|净变动|`、流出量、交易次数排序。

### 8. LP provider analysis：相关性与领先滞后

> 状态：已完成探索原型（2026-08-07）。

要做的：

- 按统一时间桶（默认 1 小时）构造曲线：
  - 池 TVL
  - 池内 LP 数量 / 活跃 LP 数量
  - LP 仓位数量
  - 池成交量
  - holder 数量
- 计算相关系数矩阵，并做时间 shift（例如 -24h 到 +24h），看 shift 后相关系数是否提高。
- 结果标注「相关性 ≠ 因果性」，并配合真实 Mint / Burn / Collect 事件解释。
- 剧烈变化用 z-score（如 |z| > 2）而不是固定绝对阈值。

交付：

- `docs/LP_CORRELATION_DESIGN.md` 和 `output-lp-correlation-demo/` 原型结果。

### 9. 地址关联分析（钱包聚类 + 资金流）

> 状态：部分完成 — 可行性文档与资金流原型已完成，钱包聚类待做。

可行性评估：

- 钱包聚类可以做，但只能给「疑似关联 + 置信度」，不能证明同一人。
- 信号来源：同一 CEX 入金地址、同一 gas 付费方、`owner()` / 合约控制方、合约创建 trace、批量 / 链式转账、相近时间相同行为。
- Dune 可以查 `ethereum.creation_traces` 和 Transfer 记录，合约创建者追踪可行。
- 资金流图可以基于现有 `transfers.json` 先做一版：节点 = 地址，边 = 聚合后的转账。

要做的：

- 先写设计文档，明确节点分类：EOA、合约、池、router、CEX。
- 先做资金流图原型，再做聚类。
- 聚类结果一律带置信度和证据列表，不下“同一个人”的结论。

## 四、建议每日安排

- **周五 08-07**：已完成 — 导师评论整理、数据口径、HOLDER_BALANCE_DESIGN、V3/V4 价格、分池 TVL、成交量、Top Movers、撤回表、LP 相关性与资金流原型、三份设计文档。
- **周六 08-08**：持有人双时间点快照 / 事件流重建，补 `balance_start / balance_end / peak_balance`。
- **周日 08-09**：撤回事件按池/USD 归一，大额钱包 USD 阈值口径。
- **周一 08-10**：dashboard 点击数据点显示各池明细，更新价格/成交量口径说明。
- **周二 08-11**：集中度阈值提示（70% / 90%），README / plan 同步。
- **周三 08-12**：钱包聚类原型（Dune creation_traces + gas payer + owner 穿透）。
- **周四 08-13**：整理文档，准备下次导师讨论。

## 五、风险与依赖

- **RPC 成本**：双时间点快照需要 Dune 历史余额表或更高 RPC 预算；先用小窗口评估。
- **Dune 表依赖**：已确认 `tokens_ethereum.balances` 是稀疏历史余额账本（带 `block_number`），
  可做期初/期末快照和窗口轨迹；`balances_ethereum.latest` 仅作兜底。
- **V4 timeline**：已修复 — Dune swap 现在保留 token 地址，V4 按 token pair 过滤，避免 PoolManager 下混池；Curve/Balancer 仍待验证。
- **相关性结果**：样本小时相关系数不稳定，只能作为探索信号，不能单独进风险分。
- **地址关联**：容易把 router / 合约中间转账误判成真实持有人，必须做节点分类。

## 六、本周验收清单

- [x] `tvl_timeline.json` 覆盖 V3 / V4，价格不再全为 0。
- [x] dashboard 有分池 TVL 多线图，并用各池 TVL/成交量明细表呈现。
- [x] 成交量按池聚合，能看出每池贡献。
- [x] `holdings.json` 区分期初 / 期末 / 净变动 / 峰值，或写明成本评估后选择的口径。
- [x] dashboard 有 Top Movers 和撤回事件表。
- [x] main pool 定义写进文档，并同时展示 TVL 主池与成交量主池。
- [x] LP 相关性 / 领先滞后原型有输出。
- [x] 地址关联可行性文档完成，资金流图有可演示原型。
- [ ] dashboard 点击数据点显示该时间点各池明细（当前用明细表代替）。
- [ ] 撤回事件按池、按目标 token / USD 归一。
- [ ] 大额钱包按 USD / 池比例定义阈值。
- [ ] 钱包聚类原型（含置信度与证据列表）。
