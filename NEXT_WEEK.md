# 下周任务

## 要解决什么

现在流水线主要靠 **RPC 扫日志**，持仓偶尔走 Dune。下周两件事：

1. **接好 Dune**：尽量让分析用到的数据都从 Dune 拉，而不是处处 eth_getLogs。
2. **找「和 Uniswap 做市不一样」的典型池**：Curve / Balancer 这类机制不同的池，跑进分析，用来对照。

---

## 1. 接入 Dune —— 数据尽量都从 Dune 抓

### 目的

不是「多一个 holdings 开关」，而是：**把数据入口统一到 Dune**（SQL / API），RPC 只做兜底或链上校验。

### 打算覆盖的数据（按优先级）


| 数据           | 现在                      | 下周目标                    |
| ------------ | ----------------------- | ----------------------- |
| 转账地址 / 持仓    | 部分 Dune + RPC balanceOf | 稳定走 Dune                |
| 池子列表（按代币）    | RPC discovery           | Dune 查出该代币所在池           |
| Swap / 流动性事件 | RPC indexer             | 能迁的迁到 Dune；迁不了的标明仍用 RPC |
| TVL / 池深度    | 链上余额近似                  | 尽量用 Dune 表或可复现查询        |


### 要做的

- 定一张「数据清单」：每类结果对应哪条 Dune SQL / 哪张表
- 封装统一查询层（执行 → 轮询 → 缓存到 `output/`）
- 无 key / 失败时怎么回退，写清楚
- 用同一个代币窗口对比：Dune 路径 vs 纯 RPC，看差在哪

### 验收

- 配置 `DUNE_API_KEY` 后，主路径数据能从 Dune 跑通
- README / `.env.example` 写清怎么用
- 知道哪些字段**暂时还不能**离开 RPC或者用RPC更方便

---

## 2. 接入和 Uniswap 做市方法不一样的典型池

### 目的

Uniswap（尤其 V2/V3）是常数积 / 集中流动性那一套。下周要专门找 **做市公式或池结构明显不同** 的典型池，接到现有分析里，方便对比：


| 类型           | 典型代表                  | 和 Uni 不一样在哪（直觉） |
| ------------ | --------------------- | --------------- |
| Stable / 锚定类 | **Curve** StableSwap  | 低滑点曲线，适合同类资产    |
| 加权多资产        | **Balancer** Weighted | 权重可调、一池多币       |
| （若有余力）       | CryptoSwap 等          | 波动对仍用非 x·y=k 曲线 |


重点不是「非 Uni 都扫一遍」，而是：**选几个机制上典型的池**，完整走完 discover → index → positions → dashboard。

### 要做的

- 选定 1–2 个示范代币（池子要真的在 Curve/Balancer 上够典型），例如 CRV / CVX / BAL
- 每个机制至少保住 **1 个典型池** 进入 `verified_pools` 并完成事件/仓位
- 看板能看出：这是 Curve / Balancer，而不只是又一个 Uniswap 池
- 在报告或笔记里用一两段话对比：和 Uni 做市差异 → 对集中度 / 撤池 / 风险解读有什么影响

### 验收

- `verified_pools.json` 里明确出现 curve 和/或 balancer 的典型池
- 端到端（或 discover + dashboard）可复现
- 有一段「和 Uniswap 做市差异」的简短说明（README 或 `research-notes/`）

---

## 怎么串起来

```text
Dune 统一拉数
    → 列出代币在各 DEX 的池（含机制类型）
    → 挑出「非 Uni 做市」的典型池
    → 喂进现有验证 / 索引 / 仓位 / 看板
```

Dune 是数据入口；「不同做市方式的典型池」是分析对象。

---

## 先不做

- 多链
- 精确到美元的完整预言机体系（近似 TVL 即可）
- 路由 / 受益人深度 unwrap

---

## 验收时填一下


| 项                  | 结果  |
| ------------------ | --- |
| Dune 主路径拉数         |     |
| 仍依赖 RPC 的部分        |     |
| 典型非 Uni 池（地址 + 协议） |     |
| 示范代币与输出目录          |     |


